# Notifications

Documentation, demo operations, and submission assets only.

No service code lives here. Email templates used at runtime live in `notifications/templates/` and are referenced by the backend notification service.
